import { UserType } from "@/lib/types/userTypes";

type Props = {
    user: UserType;
}

export default function CompleteProfileOTP({ user }: Props) 
{
    return (
        <div>
            <h1>Complete OTP</h1>
        </div>
    )
}